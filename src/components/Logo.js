import React from "react";
import Logoo from "../assets/logo.svg";

const Logo = () => {
  return (
    <div className="logo">
      <img className="logoImg" src={Logoo} alt="logo" />
    </div>
  );
};

export default Logo;
